# mini-shell
mini-shell project

## installation
compile it using theses commands:
```
git clone https://github.com/4re5group/mini-shell.git
cd mini-shell
gcc main.c -o mini-shell || exit 1
clear && chmod +x mini-shell && ./mini-shell
```